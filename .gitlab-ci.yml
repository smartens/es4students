# https://raw.githubusercontent.com/brianegan/flutter_redux/master/.gitlab-ci.yml
stages:
  - test
  - deploy

unit_test:
  image: git.uni-due.de:6666/project-flutter/docker:latest
  stage: test
  script:
    - flutter test --coverage
    - genhtml coverage/lcov.info --output=coverage
    - dartdoc
  artifacts:
    paths:
      - coverage/
      - doc/api/

pages:
  image: alpine
  stage: deploy
  dependencies:
    - unit_test
  script:
    - mkdir public
    - mv coverage/ public/coverage/
    - mv doc/api/ public/api/
  artifacts:
    paths:
      - public
  only:
    - master